import styles from '../styles/StyledBox.module.css';

export default function StyledBox() {
  return (
    <div className={styles.Wrapper}>
      <p className={styles.Title}>Some text</p>
    </div>
  );
}
